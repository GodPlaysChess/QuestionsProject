Answer - (answer_id, question_id, answer_text, user_id)
класс User(user_id, user_name)
и для них тоже методы ДАО и сервисы (пока простейшие, добавить, изменить, удалить)

тебе надо заботать, как в хибернейте замапить запрос с joinом на класс

1) List одним запросом
2) Дописать контроллеры  saveQuestion -> addQuestion

 например id вопроса
 делаешь input type="hidden"

вот что тебе точно нужо - так это попробовать запилить контроллер
на урл savequestion.html метод POST параметр  метода - класс Question
спринг его самсоберет так как в форме я называл поля так же как и в классе
name="id" name="text"  name="type"
то есть контроллер принимает запрос,
отдает его серверу, а тот в свою очередь отдает DAO методу update
так вопрос в базе и обновится

это достигается с помощью RedirectView
по моему у него вполне понятный конструктор от урла
то есть возвращаешь из метода контроллера не ModelAndView
а RedirectView
 с урлом question.html


если умеешь выполнять произвольный sql запрос то первый случай
where id in (12, 23, 54)

[19/07/2013 17:38:54] Александр Саванович: OFFSET 10
LIMIT 50