
 и setTimeout - это стандартная js

 для этого и пригодится второй метод сервиса
 соответсвенно нужен будет еще и второй метод в контроллере
 с сервисом напервый беглый взгляд все норм
 но есть одна неточность
 в базе вопрос уже может быть
 тогда надо вместо return answerDAO.insert(answer);

 инсерт упадет с PersistantException
и про setCode я написал в комменте
 то есть еще раз обобщу.
 с тем как подтягивается бустрап) подтяг jquery.js
 далее на странице должен быть <script></script>
 внутри которого будет вся логика автосэйва
 логика следущая - находим форму var sendForm = $(form);
 сериализуем
var data = sendForm.serialize();
 отсылаем аjaxом
 (заботать параметры, как указать урл и данные)
 гуглить по jquery ajax
 и все это оборачиваем в setTimeout и отсылаем каждые 5 секунд
 (типа если пользователь зазевается, окно закроет - все что он полчаса набивал не потеряется)